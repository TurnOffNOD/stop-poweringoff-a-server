This is a very simple script. Obviously there is no need to use version control.
However,in order to read it conviently, I use git to upload it to a server.

Usage: alias 'poweroff=$(path to this script)'
